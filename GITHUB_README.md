# 學習、社交及情緒適應問卷

這是一個純前端 React + Vite 專案。程式碼包含評分、備註、平均分數分析、訓練目標、Excel 匯出／匯入及列印 PDF 功能。

## 上傳至 GitHub

將此資料夾內的所有檔案上傳至新的 GitHub repository。請不要上傳 `node_modules/`、`dist/` 或 `.manus-logs/`。

## 本機執行

```bash
pnpm install
pnpm dev
```

## 生產版本檢查

```bash
pnpm check
pnpm build
```

此專案不需要自行建立後端或資料庫；評分和分析會在瀏覽器內執行。Excel 匯入適用於本系統匯出的相容工作簿。
